pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'this is build stage'
            }
        }

        stage('Test') {
            steps {
                echo 'this is build stage'
                '''
            }
        }

        stage( 'release') {
        steps {
        echo 'this is release stage'
        }

    }

}
